findit/
├── index.html              # Main entry point (UI, catalog grid, navigation)
├── manifest.json           # PWA metadata (name, icons, theme colors, install support)
├── service-worker.js       # Offline caching, background sync, installability
├── /js
│   ├── app.js              # Core logic: catalog CRUD, gestures, export/import, reset
│   ├── db.js               # IndexedDB/local DB wrapper for unlimited storage
│   ├── ui.js               # UI rendering, dark mode toggle, swipe navigation, previews
│   └── i18n.js             # Language switching logic (English/Hebrew)
├── /css
│   └── style.css           # Styles: responsive layout, dark/light mode, 3D GUI
├── /img
│   ├── icon-192.png        # PWA icon (192px)
│   └── icon-512.png        # PWA icon (512px)
├── /lang
│   ├── en.json             # English translations
│   └── he.json             # Hebrew translations




📱 PWA Responsive Native App — Final Specification Prompt
🔹 General
Progressive Web App (PWA) with responsive design

Runs natively across Desktop, Android, iOS

Hosted on GitHub Pages → avisahy.github.io/findit

Built with HTML, CSS, JavaScript only (frameworks optional, not required)

🎨 User Interface
Modern GUI: Neumorphism / CSS 3D transforms with lightweight animations (target 60fps)

Dark mode toggle with 🌞 / 🌙 icons

Header branding: app name in top corner → links back to main page

Navigation: swipe gestures (left/right), keyboard arrows, and touch support

Full‑screen image preview with pinch‑to‑zoom and double‑tap zoom

🌍 Languages
Localization toggle: English ↔ Hebrew

All UI text, buttons, and error messages fully translated

⚙️ Core Features
Catalog management: add, edit, delete items with pictures

Image input: select from gallery or capture via camera API

Data portability: export/import catalog in JSON + CSV formats with schema validation

Reset option: clears all local data → fresh state

Offline storage: IndexedDB preferred (large capacity within browser quota), fallback to localStorage

📑 Pages
About page: app description + version info

Settings page: language toggle, dark mode, reset, import/export

Add/Edit item page: form with image upload, text fields, validation

🛠️ Technical Notes
PWA installability:

Manifest with app name, icons (192px, 512px), theme color, start URL

Service worker with cache strategies:

Cache‑first for static assets (CSS, JS, icons)

Network‑first for catalog data

Responsive layout: flexbox + CSS grid, optimized for mobile and desktop

Accessibility: WCAG 2.1 AA compliance, ARIA roles, keyboard navigation, high‑contrast mode

Performance: lazy‑load images, minimize bundle size, target <100ms input response

CI/CD: GitHub Actions → lint, test, auto‑deploy to GitHub Pages

