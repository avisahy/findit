findit/
├── index.html              # Main entry point (UI, catalog grid, navigation)
├── manifest.json           # PWA metadata (name, icons, theme colors, install support)
├── service-worker.js       # Offline caching, background sync, installability
├── /js
│   ├── app.js              # Core logic: catalog CRUD, gestures, export/import, reset
│   ├── db.js               # IndexedDB/local DB wrapper for unlimited storage
│   ├── ui.js               # UI rendering, dark mode toggle, swipe navigation, previews
│   └── i18n.js             # Language switching logic (English/Hebrew)
├── /css
│   └── style.css           # Styles: responsive layout, dark/light mode, 3D GUI
├── /img
│   ├── icon-192.png        # PWA icon (192px)
│   └── icon-512.png        # PWA icon (512px)
├── /lang
│   ├── en.json             # English translations
│   └── he.json             # Hebrew translations




📱 PWA Responsive Native App Specification
🔹 General
Progressive Web App (PWA) with responsive design

Runs natively across Desktop, Android, iOS

Hosted on GitHub Pages at avisahy.github.io/findit

Built with HTML, CSS, JavaScript only (no frameworks required unless optional)

🎨 User Interface
Modern 3D‑style GUI with smooth animations

Dark mode toggle (sun 🌞 / moon 🌙 icons)

Website name in top corner → links back to main page

Swipe gestures (left/right) and keyboard arrows for navigation

Full‑screen picture preview with zoom support

🌍 Languages
English / Hebrew toggle for full localization

⚙️ Core Features
Catalog items with pictures (add, edit, delete)

Image selection from gallery or take picture with camera

Export / Import catalog data (JSON/CSV)

Full reset option → clears all data as if first login

Local database with no storage limit (IndexedDB or similar)

📑 Pages
About page

Settings page

Add / Edit item page

🛠️ Technical Notes
Use IndexedDB or localStorage for offline catalog storage

Ensure installability as a PWA (manifest + service worker)

Responsive layout with flexbox / grid

Optimize for touch + desktop input

GitHub Actions or Pages for deployment
